# Core perception
numpy>=1.24.0
opencv-python>=4.8.0       # use opencv-python-headless on Jetson
Pillow>=10.0.0

# Inference (install TensorRT from JetPack SDK on Jetson, not pip)
onnxruntime>=1.16.0        # CPU fallback; use onnxruntime-gpu for CUDA EP
onnx>=1.14.0
onnxsim>=0.4.33            # ONNX graph simplification

# API server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
websockets>=12.0

# Privacy / encryption
cryptography>=41.0.0

# Monitoring
psutil>=5.9.0              # memory monitoring

# Training dependencies (development only — not needed on edge device)
# torch>=2.1.0
# torchvision>=0.16.0
# albumentations>=1.3.1
# ultralytics>=8.0.230      # for YOLOv8/v9 training utilities

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0

# Config
PyYAML>=6.0.1

# Optional: structured logging
python-json-logger>=2.0.7
